<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <changeSet id="v1-06-create-login-event" author="diary">

        <!-- ===================== -->
        <!-- TABLE: login_event -->
        <!-- ===================== -->

        <createTable tableName="login_event">

            <!-- ✅ BaseEntity -->
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <!-- ===================== -->
            <!-- ENTITY FIELDS -->
            <!-- ===================== -->

            <column name="user_id" type="BIGINT"/>

            <column name="ip" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="user_agent" type="VARCHAR(1000)">
                <constraints nullable="false"/>
            </column>

            <column name="success" type="BOOLEAN">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <!-- ===================== -->
        <!-- INDEXES -->
        <!-- ===================== -->

        <!-- ✅ Быстро получать историю входов пользователя -->
        <createIndex indexName="idx_login_event_user"
                     tableName="login_event">
            <column name="user_id"/>
        </createIndex>

        <!-- ✅ Аналитика по IP (брутфорс, атаки) -->
        <createIndex indexName="idx_login_event_ip"
                     tableName="login_event">
            <column name="ip"/>
        </createIndex>

        <!-- ✅ Фильтрация успешных / неуспешных входов -->
        <createIndex indexName="idx_login_event_success"
                     tableName="login_event">
            <column name="success"/>
        </createIndex>

        <!-- ✅ Для временных выборок -->
        <createIndex indexName="idx_login_event_created"
                     tableName="login_event">
            <column name="created_at"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
