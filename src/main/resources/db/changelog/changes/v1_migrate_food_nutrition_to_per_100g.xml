<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <changeSet id="v1-migrate-food-nutrition-to-per-100g" author="codex">
        <sql>
            UPDATE general_food
            SET protein = ROUND(protein * 100, 2),
                fat = ROUND(fat * 100, 2),
                carbs = ROUND(carbs * 100, 2),
                callories = ROUND(callories * 100, 2)
        </sql>

        <sql>
            UPDATE user_food
            SET protein = ROUND(protein * 100, 2),
                fat = ROUND(fat * 100, 2),
                carbs = ROUND(carbs * 100, 2),
                callories = ROUND(callories * 100, 2)
        </sql>
    </changeSet>
</databaseChangeLog>
