<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <changeSet id="v1-create-tag-suggestion" author="diary">

        <createTable tableName="tag_suggestion">

            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="tag_name" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>

            <column name="user_count" type="INT">
                <constraints nullable="false"/>
            </column>

            <column name="last_seen_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <!-- Индекс по last_seen_at -->
        <createIndex indexName="idx_tag_suggestion_last_seen" tableName="tag_suggestion">
            <column name="last_seen_at"/>
        </createIndex>

        <!-- Уникальность имени -->
        <addUniqueConstraint
                tableName="tag_suggestion"
                columnNames="tag_name"
                constraintName="uq_tag_suggestion_name"/>

    </changeSet>
</databaseChangeLog>
