<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.25.xsd">

    <changeSet id="sqlite-baseline-030-metric-name-unit-link" author="codex" dbms="sqlite" context="desktop">
        <sql splitStatements="true" stripComments="true" endDelimiter=";">
            CREATE TABLE metric_name_unit_link (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                created_at TIMESTAMP NOT NULL,
                updated_at TIMESTAMP NOT NULL,
                metric_name_id BIGINT NOT NULL,
                metric_unit_id BIGINT NOT NULL,
                CONSTRAINT uk_metric_name_unit_link_metric_name_metric_unit UNIQUE (metric_name_id, metric_unit_id),
                CONSTRAINT fk_metric_name_unit_link_metric_name
                    FOREIGN KEY (metric_name_id)
                    REFERENCES dictionary_item (id),
                CONSTRAINT fk_metric_name_unit_link_metric_unit
                    FOREIGN KEY (metric_unit_id)
                    REFERENCES dictionary_item (id)
            );

            CREATE INDEX idx_metric_name_unit_link_metric_name ON metric_name_unit_link (metric_name_id);
            CREATE INDEX idx_metric_name_unit_link_metric_unit ON metric_name_unit_link (metric_unit_id);
        </sql>
    </changeSet>
</databaseChangeLog>
